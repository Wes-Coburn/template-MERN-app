{
  "name": "template-mern-app",
  "version": "1.0.0",
  "description": "Template for a full-stack MERN app",
  "scripts": {
    "test": "cd server && npm test & cd ../client && npm test",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start",
    "install-update": "npm i && ncu && ncu -u && npm i",
    "install-update-all": "npm run install-update && cd server && npm run install-update && cd ../client && npm run install-update"
  },
  "devDependencies": {
    "npm-check-updates": "^16.14.6"
  }
}
