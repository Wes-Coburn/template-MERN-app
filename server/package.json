{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "echo \"Error: no formatter specified\" && exit 1",
    "i-ncu": "npm i && ncu && ncu -u && npm i"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21"
  },
  "engines": {
    "node": ">=18.18.0 <19.0.0"
  }
}
